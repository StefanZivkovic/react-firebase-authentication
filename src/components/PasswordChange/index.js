import React from "react";
import { Typography } from "@material-ui/core";

const PasswordChange = () => (
  <div>
    <Typography>PasswordChange</Typography>
  </div>
);
export default PasswordChange;
